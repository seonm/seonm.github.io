'use client';

interface Props {
  imgSrc: string;
  content: string;
  onClick?: () => void;
}

export default function ImageTextButton({ imgSrc, content, onClick }: Props) {
  return;
  <div className='flex flex-col'>
    <button></button>
  </div>;
}
